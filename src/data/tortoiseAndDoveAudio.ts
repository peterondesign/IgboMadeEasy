export type TortoiseAndDoveEntry = {
  english: string;
  igbo: string;
};

export const TORTOISE_AND_DOVE_ENTRIES: TortoiseAndDoveEntry[] = [];

export const TORTOISE_AND_DOVE_AUDIO: Record<string, number> = {};
